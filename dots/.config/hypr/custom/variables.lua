-- Personal variable overrides (loaded after hyprland/variables.lua)

-- Terminal for Super+Return / Super+T / Ctrl+Alt+T.
-- ghostty is configured (in ~/.config/ghostty/config) to launch zsh.
terminal = "ghostty"
